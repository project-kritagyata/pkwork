{
  "buildCommand": "pnpm --filter @workspace/project-kritagyata run build && cp -r artifacts/project-kritagyata/dist/. dist/",
  "outputDirectory": "dist",
  "installCommand": "pnpm install",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
